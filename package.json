{
  "name": "superblock-site",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/assert-clean.mjs",
    "preview": "astro preview",
    "deploy": "astro build && wrangler deploy",
    "check": "node scripts/assert-clean.mjs"
  },
  "dependencies": {
    "astro": "^7.3.1"
  },
  "devDependencies": {
    "wrangler": "^4.129.0"
  }
}
